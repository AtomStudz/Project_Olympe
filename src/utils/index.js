// export const getChallengeById = (id, challenges) => {
//   const challenge = challenges.find((item) => item.id);
//   return challenge;
// }
// const challenge = challenges.find((item) =>
//     item.slug == slug
//   );
